// PLEASE NOTE: Import storage.config.ts first to ensure the storage is set before any other imports
import './src/storage.config';
import { registerRootComponent } from 'expo';
import App from './App';
registerRootComponent(App);
