export enum OrderStatus {
  PENDING = 'PENDING',
  CONFIRMED = 'CONFIRMED',
  COMPLETED = 'COMPLETED',
  EXPIRED = 'EXPIRED',
  CANCELED = 'CANCELED',
}

export enum OrderScheduleStatus {
  OUT_OF_SCHEDULE = 'OUT_OF_SCHEDULE',
}
